# This file exists to prevent Render.com from detecting Python
# This project uses Node.js, not Python
# The actual dependencies are in package.json

from setuptools import setup

setup(
    name="azure-diagram-generator",
    version="1.0.0",
    description="This project uses Node.js, not Python",
    py_modules=[],
    install_requires=[],
)
