# Variables de entorno para Diagramas Creator
# Copia este archivo como .env y configura tus valores

# API Key de OpenAI (requerido para generación de diagramas con IA)
# Obtén tu key en: https://platform.openai.com/api-keys
OPENAI_API_KEY=tu-api-key-de-openai-aqui

# Clave secreta para sesiones de Flask (opcional)
# Puedes generar una con: python -c "import secrets; print(secrets.token_hex(16))"
SECRET_KEY=tu-clave-secreta-aqui

# Configuración del servidor (opcional)
FLASK_ENV=development
FLASK_DEBUG=True
FLASK_HOST=0.0.0.0
FLASK_PORT=5000

# Configuración de archivos (opcional)
MAX_FILE_SIZE=16777216  # 16MB en bytes
UPLOAD_FOLDER=uploads
OUTPUT_FOLDER=outputs

# Configuración de OpenAI (opcional)
OPENAI_MODEL=gpt-4
OPENAI_MAX_TOKENS=2000
OPENAI_TEMPERATURE=0.7
